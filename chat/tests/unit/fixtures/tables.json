{"tables":[{"id":"customer_identity/dim_customers","name":"dim_customers","domainId":"customer_identity","kind":"dimension","grain":"One row per customer.","conformed":false,"columnCount":8,"description":"The Customer aggregate root. One row per person the shop knows about, carrying the lifetime totals that summarise their order history. Ordering borrows this dimension across the context boundary rather than documenting its own copy, which is the dependency the context map records."},{"id":"customer_identity/dim_date","name":"dim_date","domainId":"customer_identity","kind":"dimension","grain":"One row per calendar date.","conformed":true,"columnCount":6,"description":"A local calendar copy that predates the Shared Kernel, kept because the cohort reports were written against its fiscal columns. It carries fewer calendar attributes than the kernel's `dim_date` and two the kernel does not have, which is exactly the drift a conformed dimension is supposed to prevent."},{"id":"ordering/fact_order_items","name":"fact_order_items","domainId":"ordering","kind":"fact","grain":"One row per product on an order.","conformed":false,"columnCount":8,"description":"The line items of an order. It sits inside the Order aggregate, so it is reached through `fact_orders` rather than queried on its own; the price and supply cost are captured as they were when the item was sold."},{"id":"ordering/fact_orders","name":"fact_orders","domainId":"ordering","kind":"fact","grain":"One row per order.","conformed":false,"columnCount":15,"description":"The Order aggregate root: one row per order placed, with the money and the item counts already rolled up from its line items. This is the fact every other context reads when it wants to know what the shop sold."},{"id":"product_catalog/dim_products","name":"dim_products","domainId":"product_catalog","kind":"dimension","grain":"One row per product in the catalog.","conformed":false,"columnCount":7,"description":"The Product aggregate root: one row per item on the menu, carrying the current catalog price and whether it is food or drink. Ordering borrows this dimension across the context boundary; it does not document its own copy."},{"id":"product_catalog/dim_supplies","name":"dim_supplies","domainId":"product_catalog","kind":"dimension","grain":"One row per supply per product.","conformed":false,"columnCount":6,"description":"The components that go into a product. A supply used by three products appears three times, once per product, because its cost is stated per product rather than per supply."},{"id":"product_catalog/fact_supply_cost_snapshot","name":"fact_supply_cost_snapshot","domainId":"product_catalog","kind":"fact","grain":"One row per product per supply per day.","conformed":false,"columnCount":7,"description":"A daily snapshot of what each product costs to supply and what margin it earns at the current catalog price. Written up ahead of the dimensions it will join, so its relationships are still described in prose rather than declared against table documents."},{"id":"shared_kernel/dim_date","name":"dim_date","domainId":"shared_kernel","kind":"dimension","grain":"One row per calendar date.","conformed":true,"columnCount":10,"description":"The conformed calendar dimension. Every context that has a date joins this one, which is what makes a day comparable across context boundaries. Built from the dbt project's time spine so the calendar cannot drift away from the spine the metrics layer already uses."},{"id":"store_operations/dim_locations","name":"dim_locations","domainId":"store_operations","kind":"dimension","grain":"One row per store.","conformed":false,"columnCount":4,"description":"The Location aggregate root: one row per shop, with the tax rate that applies to orders taken there. Ordering borrows this dimension across the context boundary to attribute an order to a store."},{"id":"store_operations/fact_location_daily_sales","name":"fact_location_daily_sales","domainId":"store_operations","kind":"fact","grain":"One row per store per day.","conformed":false,"columnCount":9,"description":"Daily performance per store, aggregated from the orders the Ordering context publishes. A store with no orders on a day still gets a row, with zeroes, so the series is continuous."}]}
