apiVersion: v1
kind: Service
metadata:
  name: chat
  labels:
    app.kubernetes.io/name: chat
    app.kubernetes.io/component: chat
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: chat
  ports:
    - name: http
      port: 8090
      targetPort: http
---
# The model credential is ADC, so the pod needs an identity of its own: on GKE,
# annotate this with iam.gke.io/gcp-service-account and the token comes from the
# metadata server, nothing mounted. The default ServiceAccount cannot be
# annotated without handing that identity to every other pod in the namespace.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: chat
  labels:
    app.kubernetes.io/name: chat
    app.kubernetes.io/component: chat
# The service calls no Kubernetes API. Workload Identity is unaffected: its
# token comes from the metadata server, not this volume.
automountServiceAccountToken: false
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat
  labels:
    app.kubernetes.io/name: chat
    app.kubernetes.io/component: chat
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: chat
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: chat
        app.kubernetes.io/component: chat
    spec:
      serviceAccountName: chat
      # Must exceed ANSWER_TIMEOUT_SECONDS, or a rollout kills a turn mid-answer.
      terminationGracePeriodSeconds: 150
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      volumes:
        # The root filesystem is read-only, and Python still wants somewhere to
        # write. A local cluster has no metadata server, so the dev overlay adds
        # an ADC file volume next to this one; prod must not, or the file
        # shadows Workload Identity.
        - name: tmp
          emptyDir: {}
      containers:
        - name: chat
          image: urara-vision/chat:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8090
          envFrom:
            - configMapRef:
                name: relviz-chat-config
          env:
            - name: TMPDIR
              value: /tmp
            # The same secret the backend reads as API_TOKEN, so there is one
            # token to rotate rather than two.
            - name: BACKEND_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: relviz-api
                  key: token
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          # /readyz checks the backend, so a backend outage takes chat pods out
          # of service rather than leaving them accepting questions they cannot
          # answer. /healthz deliberately does not: kubelet must never restart a
          # chat pod because its dependency is down.
          startupProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 5
            failureThreshold: 60
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              # 105Mi measured peak at MAX_CONCURRENT_TURNS=4; the rest is
              # headroom for a larger corpus.
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: relviz-chat-config
data:
  APP_ADDR: ":8090"
  BACKEND_BASE_URL: "http://backend:8080"
  BACKEND_TIMEOUT_SECONDS: "30"
  LOG_LEVEL: "info"
  # Vertex authenticates with ADC, so no key belongs here or in any secret.
  LLM_PROVIDER: "vertex"
  LLM_MODEL: "gemini-2.5-flash"
  LLM_TEMPERATURE: "0.2"
  LLM_MAX_OUTPUT_TOKENS: "2048"
  LLM_TIMEOUT_SECONDS: "60"
  # Set per overlay; the pod refuses to start without it.
  VERTEX_PROJECT: ""
  VERTEX_LOCATION: "us-central1"
  MAX_TOOL_ITERATIONS: "6"
  MAX_HISTORY_MESSAGES: "20"
  MAX_CONCURRENT_TURNS: "4"
  MAX_QUESTION_CHARS: "4000"
  MAX_REQUEST_BYTES: "1048576"
  TURN_ADMISSION_WAIT_SECONDS: "0.5"
  # Bounded by terminationGracePeriodSeconds above.
  ANSWER_TIMEOUT_SECONDS: "120"
  CONTEXT_CACHE_TTL_SECONDS: "300"
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: chat
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: chat
